info:
  name: Get GROQ Models
  type: http
  seq: 1

http:
  method: GET
  url: https://api.groq.com/openai/v1/models
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

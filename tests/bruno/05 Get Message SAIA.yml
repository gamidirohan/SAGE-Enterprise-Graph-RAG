info:
  name: Get Message SAIA
  type: http
  seq: 6

http:
  method: GET
  url: http://127.0.0.1:8000/api/messages/{{message_id}}/saia
  headers:
    - name: X-User-Id
      value: "{{user_id}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
